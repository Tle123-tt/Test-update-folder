import { createSlice } from "@reduxjs/toolkit";

const initialState={
    prduct:[],
}

const prduct=createSlice({
    name:'prduct',
    initialState,
    reducers:{

    }
});

export default prduct.reducer

